const {bubble_sortASC, bubble_sortDESC} = require("./test")

let  arraybs = [9, 3, 5, 4, 1, 7];
console.log(arraybs);

// Ascending
bubble_sortASC(arraybs);
console.log(arraybs);

// Descending
bubble_sortDESC(arraybs);
console.log(arraybs);


